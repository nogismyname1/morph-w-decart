# Initialize a new git repository in your folder
git init -b main

# Add all your files to be tracked
git add .

# Make your first save (commit)
git commit -m "First commit of my Decart app"

# Link your local folder to the GitHub repository you created
git remote add origin [YOUR_GITHUB_REPOSITORY_URL.git] 
# (Copy the URL from your GitHub repo page)

# Push your code to GitHub
git push -u origin main

npm install vite --save-dev
